@page "/signup"

<EditForm Model="userInfo" OnValidSubmit="CreateUser">
    <div class="login div1">
        <div class="logo1div">
            <img class="logo1" src="Images/signupwork.svg" />
        </div>

        <div class="login-h2">
            <h2> Get the answers you need</h2>
        </div>
        <div class="login-pitch">
            <p>
                Get the feedback you need using the best result analysis
                platform.
            </p>
        </div>
    </div>


    <div class="login ssdiv2">
        <div class="login-logo">
            <img src="Images/logo.svg" />
        </div>


        <div class="sdiv2-child2">
            <h2>Sign Up</h2>
            <p>Enter your details to create your account</p>
            <div class="form-group">
                <div>
                    <InputText id="s-username" class="form-control border-secondary" placeholder="Fullname" @bind-Value="@userInfo.FullName" />
                </div>
            </div>
            <div class="form-group">
                <div>
                    <InputText id="s-useremail" class="form-control border-secondary" placeholder="Email" @bind-Value="@userInfo.Email" />
                </div>
            </div>
            <div class="form-group">
                <div>
                    <InputText id="s-useremail" type="password" class="form-control border-secondary" placeholder="Password" @bind-Value="@userInfo.Password" />
                </div>
            </div>
            <div class="form-group">
                <div>
                    <InputText id="s-useremail" type="password" class="form-control border-secondary" placeholder="Confirm Password" @bind-Value="@userInfo.Password" />
                </div>
            </div>

            <div class="login-sign">
                <button type="submit" class="loginsign logbutt">Sign Up</button>
            </div>

        </div>

    </div>


</EditForm>


@code {
    private UserInfo userInfo = new UserInfo();

    private void CreateUser()
    {

    }
}
